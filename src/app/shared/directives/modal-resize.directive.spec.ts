import { ModalResizeDirective } from './modal-resize.directive';

describe('ModalResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalResizeDirective();
    expect(directive).toBeTruthy();
  });
});
